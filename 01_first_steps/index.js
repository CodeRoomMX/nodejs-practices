//Declare a const named num
const num1=10;
const num2=20;

//Declare a function named add that calculates the addition of num1 plus num2
const add= (num1, num2) => (num1 + num2);
console.log(add(1,2));
