/*
 *
 * TODO:
 * Having the next declared function, complete it to be able to return the 
 * fullname from the user argument.
 * 
 */

const myUser = {
  firstName: "Code",
  lastName: "Room"
}

const getFullNameFrom = (user) => ();

const fullName = getFullNameFrom(myUser);
console.log(fullName) // Expected result: Code Room